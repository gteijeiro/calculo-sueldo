// Helper script to generate PNG icons from SVG
// Run: node public/icon-generate.mjs
// Requires: npm install sharp (optional, fallback to SVG)
// This file documents intent; icons can be generated via any SVG→PNG tool.
// The app works with SVG-only browsers without this step.
